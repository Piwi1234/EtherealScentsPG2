{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm exec turbo build --filter=@app/web..."
  },
  "deploy": {
    "startCommand": "pnpm --filter web start"
  }
}
