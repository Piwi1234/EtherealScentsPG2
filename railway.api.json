{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm exec turbo build --filter=@app/api..."
  },
  "deploy": {
    "startCommand": "pnpm --filter @app/database exec prisma migrate deploy && pnpm --filter @app/api start"
  }
}
